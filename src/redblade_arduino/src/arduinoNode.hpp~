#ifndef ARDUINO_NODE_H
#define ARDUINO_NODE_H

#include "arduino.hpp"
#include "ros/ros.h"

class arduinoNode {
public:
  arduinoNode();
  virtual ~arduinoNode(void);
  arduino* arduinoCall;
  //  void operator()();

private:


};

#endif
